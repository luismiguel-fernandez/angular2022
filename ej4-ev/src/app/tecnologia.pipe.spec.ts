import { TecnologiaPipe } from './tecnologia.pipe';

describe('TecnologiaPipe', () => {
  it('create an instance', () => {
    const pipe = new TecnologiaPipe();
    expect(pipe).toBeTruthy();
  });
});
